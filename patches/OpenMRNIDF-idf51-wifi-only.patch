diff --git a/CMakeLists.txt b/CMakeLists.txt
index 9ceae16..4f67925 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -16,8 +16,10 @@ set(IDF_DEPS
     app_update
     driver
     esp_system
+    esp_wifi
     hal
     heap
+    pthread
     vfs
     mdns)
 
@@ -26,15 +28,21 @@ if (IDF_VERSION_MAJOR LESS 5)
     list(APPEND IDF_DEPS esp_ipc)
 endif()
 
+# LEDC needs IDF APIs this snapshot does not match. TWAI HAL in IDF 5.1
+# requires a config argument; this phase is WiFi-only, so skip the CAN driver.
 idf_component_register(SRC_DIRS "${SOURCE_DIRS}"
                        INCLUDE_DIRS "src"
-                       REQUIRES "${IDF_DEPS}")
+                       REQUIRES "${IDF_DEPS}"
+                       EXCLUDE_SRCS
+                           "src/freertos_drivers/esp32/Esp32Ledc.cpp"
+                           "src/freertos_drivers/esp32/Esp32HardwareTwai.cpp")
 
 ###############################################################################
 # Add required compilation flags for OpenMRN
 ###############################################################################
 
 target_compile_options(${COMPONENT_LIB} PUBLIC -DESP32)
+target_compile_options(${COMPONENT_LIB} PRIVATE -Wno-error=format -Wno-format)
 
 ###############################################################################
 # Silence a few warnings for various files to reduce compilation noise
