# Copy to wifi_secrets.env (gitignored) and fill in locally.
# Do not commit wifi_secrets.env.
# Do not paste these values into chat or GitHub issues.

# SSID is not secret (beacons advertise it). PSK is secret — local file only.
WIFI_SSID=SRIF2333
WIFI_PASSWORD=
